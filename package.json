{
  "name": "ordenes-ineco",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix api & npm run dev --prefix web",
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "install:all": "npm install --prefix api && npm install --prefix web",
    "build": "npm run build --prefix api && npm run build --prefix web"
  }
}
