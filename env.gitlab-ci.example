# =============================================================================
# GITLAB → Settings → CI/CD → Variables
# =============================================================================
# Solo para el BUILD de la imagen Docker (al pushear un tag vX.Y.Z).
# El frontend llama a /api en el mismo origen (VITE_API_URL vacío).
# =============================================================================

VITE_API_URL=
